import {styled} from "@mui/material/styles";

export const SHeader = styled("header")(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  padding: "28px 0"
}))

export const SHeaderLogoWrap = styled("div")(() => ({
  display: "flex",
  alignItems: "center",
  gap: "45px"
}))