import {styled} from "@mui/material/styles";

export const SAwaits = styled("section")(() => ({
  paddingTop: "180px",
  paddingBottom: "60px"
}))

export const SAwaitsList = styled("ul")(() => ({
  listStyle: "none",
  margin: "60px 0 30px",
  padding: 0,
  display: "flex",
  flexDirection: "column",
  gap: "30px",
}))