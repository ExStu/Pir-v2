import {styled} from "@mui/material/styles";

export const SSpeakersItem = styled("div")(() => ({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  textAlign: "center",
  gap: "24px"
}))

export const SSpeakersItemImg = styled("img")(() => ({
  width: "261px",
  height: "261px",
  borderRadius: "50%",
}))