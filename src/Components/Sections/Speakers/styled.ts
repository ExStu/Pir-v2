import {styled} from "@mui/material/styles";
import mq from "@shared/themes/breakpoints.ts";

export const SSpeakers = styled("section")(() => mq({
  paddingTop: ["20px", "40px", "60px"],
  paddingBottom: ["80px", "105px"],
}))