import {styled} from "@mui/material/styles";

export const SPast = styled("section")(() => ({
  paddingTop: "60px",
  paddingBottom: "105px"
}))

export const SPastImg = styled("img")(() => ({
  width: "100%",
  height: "300px",
  borderRadius: "20px"
}))